# Cursor Cloud Agent — required secrets
#
# Add these in: https://cursor.com/dashboard → Cloud Agents → Secrets
# (or environment-scoped secrets for this repo's saved environment)
#
# Secrets inject at SESSION START. After adding, start a new cloud agent run.
# Agents cannot write to the Cursor dashboard from inside a running VM.
#
# ── Option A: Vercel pull (frontend vars) + Railway (backend vars) ──
VERCEL_TOKEN
VERCEL_PROJECT_ID
VERCEL_ORG_ID
DATABASE_URL
NITROGEN_FIREBASE_CREDENTIALS

# ── Stripe (repo-scoped — _NITROGEN suffix is MANDATORY) ──
# NEVER add unsuffixed STRIPE_SECRET_KEY / STRIPE_PRICE_ID / STRIPE_WEBHOOK_SECRET
# to Cursor Secrets or Railway. Always use the _NITROGEN names below.
#
# materialize_dev_env.sh and sync_secrets_to_vercel.py alias these onto the
# plain names the Nitrogen AI app reads via AliasChoices.
# Local durable sync: scripts/sync_prod_secrets_to_local.sh pulls ONLY
# STRIPE_*_NITROGEN from Railway into .env on every `dev_daemon.sh start|restart`.
#
# MUST be the LIVE key (sk_live_...), never sk_test_local / placeholders.
STRIPE_SECRET_KEY_NITROGEN
STRIPE_PUBLISHABLE_KEY_NITROGEN
STRIPE_RESTRICTED_KEY_NITROGEN   # stored for future use; not yet consumed by the app
STRIPE_PRICE_ID_NITROGEN         # live Price id (e.g. Individual $20/mo)
STRIPE_WEBHOOK_SECRET_NITROGEN   # signing secret for Railway webhook endpoint

# ── Option B: Mirror everything individually (same names as .env.example) ──
# OPENAI_API_KEY                    # often already set
# DATABASE_URL
# FIREBASE_PROJECT_ID
# NITROGEN_FIREBASE_CREDENTIALS     # or FIREBASE_SERVICE_ACCOUNT_JSON
# NEXT_PUBLIC_FIREBASE_API_KEY
# NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
# NEXT_PUBLIC_FIREBASE_PROJECT_ID
# NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
# NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
# NEXT_PUBLIC_FIREBASE_APP_ID
# NEXT_PUBLIC_API_URL                 # optional, defaults to http://localhost:8000
# NEXT_PUBLIC_BILLING_ENABLED=true

# ── Option C: Mount existing .env file ──
# NITROGEN_ENV_FILE=/path/to/.env
