# Carbon regression snapshots

Optional `source_type: regression_snapshot` fixtures for locking engine output during refactors.

All seven method packs now have **validation** fixtures under `../carbon/`. Use this directory only when you need engine-as-truth snapshots (e.g. during a large refactor) — not as proof of methodology correctness.
