# LCOE regression snapshots

Place `source_type: regression_snapshot` fixtures here to lock current engine output during refactors.

These are **not** validation fixtures — expected values come from engine capture time, not independent hand calculation.

Validation fixtures live in `../lcoe/`.
