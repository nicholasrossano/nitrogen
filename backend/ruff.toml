# Keep the pre-0.16 default rule set. Ruff 0.16 expanded defaults from 59 → 413
# rules; adopting that broader set is a separate cleanup, not a CI surprise.
# See: https://astral.sh/blog/ruff-v0.16.0
[lint]
select = ["E4", "E7", "E9", "F"]
